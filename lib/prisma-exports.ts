/**
 * Prisma client re-export wrapper
 *
 * All project files should import Prisma types and classes from this module
 * instead of directly from the generated client path.
 * This centralizes the import path so future output path changes only need
 * updating here.
 */
export * from '../prisma/generated/prisma/client';
